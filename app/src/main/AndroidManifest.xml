<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bernst.d.federico.writesbestfriend">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".core.WritersbfApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/WritersbfTheme" >

        <activity
            android:name=".views.activities.HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.StoriesActivity"
            android:label="@string/historias" />
        <activity
            android:name=".views.activities.FormActivity"
            android:label="@string/personajes" />
        <activity
            android:name=".views.activities.CharacterListActivity"
            android:label="@string/personaje" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="810f46b17b9ef6726ada8b72e6d2b4e696ae78de" />
    </application>
</manifest>
